//factory function that takes in create to-do object
function createToDo(title, description, dueDate, priority) {
    return {
        title: title,
        description: description,
        dueDate: dueDate,
        priority: priority
    };
    console.log
}

